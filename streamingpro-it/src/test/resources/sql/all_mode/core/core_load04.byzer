--%comparator=tech.mlsql.it.IgnoreResultComparator

load csv.`/csv/skipNLines.csv` where skipFirstNLines="3" and skipLastNLines="4" and header="true" as skip_n_lines;

select count(1) as total_account from skip_n_lines as skip_n_lines_account;

!assert skip_n_lines_account ''':total_account == 9''' "Failed to skip n lines";