--%comparator=tech.mlsql.it.IgnoreResultComparator

select * from customer where C_CUSTKEY > 200 as customer_filter;

select count(1) as total_account from customer_filter as customer_filter_account;

!assert customer_filter_account ''':total_account == 100''' "Failed to filter field!";
