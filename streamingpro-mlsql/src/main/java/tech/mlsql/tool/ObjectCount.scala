package tech.mlsql.tool

/**
  * 2019-05-20 WilliamZhu(allwefantasy@gmail.com)
  */
object ObjectCount {
  def main(args: Array[String]): Unit = {
//    val vm = ac.attach(connectArgs)
//    val allClasses = vm.allClasses
//    val instanceCounts = vm.instanceCounts(allClasses)
//    for (int i = 0; i < allClasses.size(); i++)
//    {
//      System.out.println(allClasses.get(i).name()
//        + ": " + instanceCounts[i]);
//    }
  }
}
