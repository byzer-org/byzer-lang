rootLogger: INFO,console

logging:
  level:
    tech.mlsql.cluster: DEBUG

appender:
  console:
    type: console
    threshold: DEBUG
    layout:
      type: consolePattern
      conversionPattern: "[%d{ISO8601}][%-5p][%-25c] %m%n"

