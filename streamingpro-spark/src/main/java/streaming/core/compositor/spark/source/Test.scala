package streaming.core.compositor.spark.source

import org.apache.spark.sql.types._

import scala.collection.mutable

/**
 * 10/18/16 WilliamZhu(allwefantasy@gmail.com)
 */
class Test {
  def a = {
    StructType(Array(StructField("", LongType, true)))
    val item = new mutable.HashMap[String,Any]()
  }
}
